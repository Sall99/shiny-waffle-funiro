export type createAccountFormValues = {
  email: string;
  password: string;
  fname: string;
  lname: string;
  confirmPassword: string;
};

export type UserInformatioFormValues = {
  email?: string;
  fname?: string;
  lname?: string;
};
